version: "2"
sql:
  - engine: "postgresql"
    queries: "identity/store/queries/"
    schema: "migrations/"
    gen:
      go:
        package: "sqlc"
        out: "gen/go/sqlc"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
