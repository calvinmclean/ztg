# Small tasks

This is a collection of small tasks that do not need a whole feature file

- [ ] Move `main.go` to a new cmd/ package
- [ ] Embed the Example keys
- [ ] Create keys in `.keys` and gitignore it
